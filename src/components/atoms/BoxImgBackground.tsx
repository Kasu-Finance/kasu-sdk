import { Box } from '@mui/material'
import { styled } from '@mui/system'

const BoxImgBackground = styled(Box)(() => ({
  backgroundImage: 'url("/images/background-header.png")',
}))

export default BoxImgBackground
