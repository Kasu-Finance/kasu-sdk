'use client'

import { Box, styled } from '@mui/material'

const CustomInnerCardContent = styled(Box)({
  borderRadius: 8,
  padding: 16,
  backgroundColor: 'white',
})

export default CustomInnerCardContent
